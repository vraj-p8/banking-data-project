name: 'banking_dbt'
version: '1.0.0'

profile: 'banking_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  banking_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
